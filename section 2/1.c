#include <stdio.h>
#include <stdlib.h>

int main()
{

    char a, b, c;

    // a. getchar statement
    a = getchar();
    b = getchar();
    c = getchar();

    // b.putchar statements
    putchar(a);
    putchar(b);
    putchar(c);

    return 0;
}